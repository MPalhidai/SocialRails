<nav class="navbar navbar-dark bg-dark">
  <div class="row justify-content-between w-100 m-0">
    <div class="col-sm-4">
      <a class="navbar-brand" href="/">SocialRails</a>
    </div>
    <div class="col-sm-6 text-right">
      <% if user_signed_in? %>
        <div class="btn-group">
          <button type="button" class="btn btn-outline-primary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <i class="fa fa-user"> <%= current_user.first_name %></i>
          </button>
          <div class="dropdown-menu dropdown-menu-right text-center">
            <%= link_to "Home", root_path %>
            <br/>
            <%= link_to "Profile", profile_path %>
            <br/>
            <%= link_to "Friends", users_friends_path %>
            <br/>
            <%= link_to "Find new friends", users_path %>
            <br/>
            <%= link_to "Notifications", users_notifications_path %>
            <br/>
            <%= link_to "Post", new_post_path %>
            <div class="dropdown-divider"></div>
            <%= link_to "Log Out", destroy_user_session_path, :method => "delete" %>
          </div>
        </div>
      <% else %>
        <%= link_to "Sign in", new_user_session_path, :class => "btn btn-outline-primary" %>
        <%= link_to "Sign up", new_user_registration_path, :class => "btn btn-outline-primary" %>
      <% end %>
    </div>
  </div>
</nav>
